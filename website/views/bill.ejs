<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../static/css/bill.css">
</head>
<body >
    <h1>ใบแจ้งหนี้</h1>
    <div class="text">
        <div class="text-row">
            <p>เล่มที่</p><p><%= condo.bill_id %></p>
            <div class="text-end"><p>วันที่</p><p><%= condo.date %></p></div>
        </div>
        <div class="text-row">
            <p>ค่าเช่าห้อง เลขที่</p><p><%= condo.room_id %></p>
            <p style="margin-left: 5%;">ประจำเดือน</p><p>มกราคม</p>
            <div class="text-end"><p>ชื่อ</p><p><%= condo.title %> <%= condo.name %> <%= condo.surname %></p></div>
        </div>
    </div>
    <table class="invoice-table">
        <thead>
            <tr>
                <th class="table-list">รายการ</th>
                <th class="table-amount">จำนวน</th>
                <th class="table-each">หน่วยละ</th>
                <th class="table-sum">รวมเงิน</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>ค่าเช่า</td>
                <td></td>
                <td></td>
                <td><%= condo.room_price %></td>
            </tr>
            <tr>
                <td>ค่าน้ำ</td>
                <td id="wate"><%= condo.wate %></td>
                <td id="each_wate">18</td>
                <td id="total_wate"></td>
            </tr>
            <tr>
                <td>ค่าไฟ</td>
                <td id="elec"><%= condo.elec %></td>
                <td id="each_elec">8</td>
                <td id="total_elec"></td>
            </tr>
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
            <tr>
                <td class="fine"><p>ค่าปรับ</p><p><%= condo.fine_info %></p></td>
                <td></td>
                <td></td>
                <td><%= condo.fine %></td>
            </tr>
            <tr>
                <td>รวมทั้งสิ้น</td>
                <td></td>
                <td></td>
                <td><%= condo.total %></td>
            </tr>
        </tbody>
    </table>
    <div class="text">
        <div class="text-row">
            <p style="text-align: start;">หมายเหตุ:ใบเสร็จรับเงินนี้จะสมบูรณ์ เมื่อได้รับการชำระเงินเป็นที่เรียบร้อย</p>
            <img src="../static/img/qrcode.png" class="qrcode">
        </div>
        <button id="print">
            <img src="../static/img/print.png" alt="">
        </button>
    </div>

    <script>
        const printBtn = document.getElementById('print');
        printBtn.addEventListener('click', function(){
            print();
        })

        const wate = document.getElementById('wate').innerHTML;
        const each_wate = document.getElementById('each_wate').innerHTML;
        let total_wate = document.getElementById('total_wate');
        total_wate.innerHTML = parseFloat(wate)*parseFloat(each_wate);

        const elec = document.getElementById('elec').innerHTML;
        const each_elec = document.getElementById('each_elec').innerHTML;
        let total_elec = document.getElementById('total_elec');
        total_elec.innerHTML = parseFloat(elec)*parseFloat(each_elec);

    </script>
</body>
</html>